---
let { block } = Astro.props;
let url: string;

switch (block.image.type) {
  case "file":
    url = block.image.file.url;
    break;
  default:
    url = block.image.external.url;
    break;
}
---

<img class="my-10" src={url} alt={block.id} width="520" />
